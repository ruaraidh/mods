#ifndef movingpoint_h
#define movingpoint_h 
#include "point.h"
#include <cmath>

class movingpoint : public point{
public:
  movingpoint(string name);
  void update(double time);
  void connect();
};

#endif
