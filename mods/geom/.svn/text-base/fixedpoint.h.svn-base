#ifndef fixedpoint_h
#define fixedpoint_h 
#include "point.h"
#include <cmath>

class fixedpoint : public point{
public:
  fixedpoint(string name);
  void update(double time);
  void connect();
};

#endif
